import { createContext } from "react";

/**
 * Genera el userContext que se va a utilizar en el resto de vistas
 * @param {any} null
 * @returns {any}
 */
export const UserContext = createContext(null);